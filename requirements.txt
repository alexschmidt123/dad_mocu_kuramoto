# Core dependencies (RTX 4090 optimized)
# Install PyTorch with CUDA 12.1 first:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

numpy>=1.24.0
scipy>=1.9.0
PyYAML>=6.0.0
matplotlib>=3.5.0
tqdm>=4.65.0

# Note: torch should be installed separately with CUDA support
# The above command ensures RTX 4090 compatibility with CUDA 12.1